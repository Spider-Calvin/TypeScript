type objtype = {
    height: number,
    weight: number,
    gender?:string //we made the gender optional
}

const obj:objtype ={
    height:12,
    weight:30,
    gender:'male'
}

const obj1:objtype ={
    height:12,
    weight:30
}
